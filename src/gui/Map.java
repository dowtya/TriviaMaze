package gui;

public class Map {
	
	updateVisuals(gamestate); // display the current map
	
	getPlayerMovement(onPlayerMovement); // create clickable arrows to move the player; call callback with the direction selected.
	
}
